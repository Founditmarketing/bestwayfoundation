# Email service for the contact form (not wired up yet — see src/app/api/contact/route.ts)
# Recommended: Resend (https://resend.com) — add the key, then implement the TODO in the route handler.
RESEND_API_KEY=

# Where contact-form leads should be delivered once email is wired up.
CONTACT_INBOX_EMAIL=
